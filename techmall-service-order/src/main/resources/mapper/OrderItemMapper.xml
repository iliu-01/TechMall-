<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.techmall.order.mapper.OrderItemMapper">

    <resultMap id="OrderItemMap" type="com.techmall.order.entity.OrderItem">
        <id property="id" column="id"/>
        <result property="orderId" column="order_id"/>
        <result property="productId" column="product_id"/>
        <result property="merchantId" column="merchant_id"/>
        <result property="productName" column="product_name"/>
        <result property="productPrice" column="product_price"/>
        <result property="quantity" column="quantity"/>
        <result property="amount" column="amount"/>
    </resultMap>

    <insert id="insert" parameterType="com.techmall.order.entity.OrderItem"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO t_order_item (order_id, product_id, merchant_id, product_name,
                                  product_price, quantity, amount)
        VALUES (#{orderId}, #{productId}, #{merchantId}, #{productName},
                #{productPrice}, #{quantity}, #{amount})
    </insert>

    <select id="selectByOrderId" resultMap="OrderItemMap">
        SELECT * FROM t_order_item WHERE order_id = #{orderId}
    </select>

</mapper>
