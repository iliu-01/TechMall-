<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.techmall.product.mapper.CategoryMapper">

    <resultMap id="categoryMap" type="com.techmall.product.entity.Category">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="parentId" column="parent_id"/>
        <result property="sort" column="sort"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <select id="selectAll" resultMap="categoryMap">
        SELECT id, name, parent_id, sort, created_at
        FROM t_category
        ORDER BY parent_id, sort
    </select>

    <select id="selectById" resultMap="categoryMap">
        SELECT id, name, parent_id, sort, created_at
        FROM t_category
        WHERE id = #{id}
    </select>

    <insert id="insert" parameterType="com.techmall.product.entity.Category"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO t_category (name, parent_id, sort, created_at)
        VALUES (#{name}, #{parentId}, #{sort}, NOW())
    </insert>

</mapper>
